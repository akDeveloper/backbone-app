define([
    'underscore',
    'backbone',
], function(_, Backbone){
    var PostModel = Backbone.Model.extend({
        initialize: function() {
        },
        defaults: {
        }
    });

    return PostModel;
});
