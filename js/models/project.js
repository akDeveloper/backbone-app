define([
    'underscore',
    'backbone',
], function(_, Backbone){
    var ProjectModel = Backbone.Model.extend({
        initialize: function() {
        },
        defaults: {
        }
    });

    return ProjectModel;
});
